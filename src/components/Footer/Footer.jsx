import style from './style.module.scss';

export const Footer = () => {
  return (
    <footer className={style.root}>
      <h2 className={style.title}>We are available in 10 Cities</h2>
    </footer>
  );
};
